const STORAGE_KEY = 'printb-data-v1';
const seed = { sales: [], clients: [], payments: [] };
let data = JSON.parse(localStorage.getItem(STORAGE_KEY) || 'null') || seed;
data.printers = data.printers || [{ id: crypto.randomUUID(), name: 'Epson L220', status: 'Lista para imprimir', isDefault: true }];
const money = value => new Intl.NumberFormat('es-DO', { style: 'currency', currency: 'DOP' }).format(Number(value) || 0);
const saleOptions = [
  { name: 'Blanco y negro – texto', prices: [5, 4, 3] },
  { name: 'B/N – con imágenes/gráficos', prices: [7, 6, 5] },
  { name: 'Color – texto / poco color', prices: [10, 8, 7] },
  { name: 'Color – gráficos / color medio', prices: [15, 12, 10] },
  { name: 'Color – página completa / mucha tinta', prices: [20, 18, 15] },
   { name: 'Imagen/foto en papel normal', prices: [25, 20, 18] },
   { name: 'Copia de cédula – B/N', prices: [10, 10, 10] },
   { name: 'Copia de cédula – color', prices: [20, 20, 20] }
];
const priceForPages = (optionIndex, pages) => { const option = saleOptions[optionIndex]; const range = pages <= 10 ? 0 : pages <= 50 ? 1 : 2; return option ? option.prices[range] * pages : 0; };
const save = () => { localStorage.setItem(STORAGE_KEY, JSON.stringify(data)); if (window.PRINTB_CLOUD_SAVE) window.PRINTB_CLOUD_SAVE(data); };
const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#039;' }[char]));
const clientById = id => data.clients.find(client => client.id === id);
const defaultPrinter = () => data.printers.find(printer => printer.isDefault) || data.printers[0];
const clientDebt = id => data.sales.filter(sale => sale.clientId === id).reduce((sum, sale) => sum + sale.total - sale.paid, 0);
const showToast = message => { const toast = document.getElementById('toast'); toast.textContent = message; toast.classList.add('show'); setTimeout(() => toast.classList.remove('show'), 2400); };

function navigate(view) {
  document.querySelectorAll('.view').forEach(element => element.classList.toggle('active', element.id === `${view}-view`));
  document.querySelectorAll('.nav-item').forEach(element => element.classList.toggle('active', element.dataset.view === view));
  const titles = { dashboard: 'Resumen de tu negocio', sales: 'Ventas', clients: 'Clientes y saldos', payments: 'Abonos recibidos' };
  document.getElementById('page-title').textContent = titles[view];
}
function openModal(content) { document.getElementById('modal').innerHTML = content; document.getElementById('modal-backdrop').classList.add('open'); }
function closeModal() { document.getElementById('modal-backdrop').classList.remove('open'); }
function dateLabel(date) { return new Intl.DateTimeFormat('es-MX', { day:'2-digit', month:'short', year:'numeric' }).format(new Date(date + 'T12:00:00')); }
function saleStatus(sale) { return sale.paid >= sale.total ? ['Pagada', 'paid'] : sale.paid > 0 ? ['Abono parcial', 'partial'] : ['Pendiente', 'pending']; }

function renderDashboard() {
  const total = data.sales.reduce((sum, sale) => sum + sale.total, 0), paid = data.sales.reduce((sum, sale) => sum + sale.paid, 0);
  document.getElementById('metric-sales').textContent = money(total); document.getElementById('metric-sales-count').textContent = `${data.sales.length} venta${data.sales.length === 1 ? '' : 's'} registrada${data.sales.length === 1 ? '' : 's'}`;
  document.getElementById('metric-profit').textContent = money(data.sales.reduce((sum, sale) => sum + sale.total - sale.cost, 0)); document.getElementById('metric-debt').textContent = money(total - paid); document.getElementById('metric-debt-count').textContent = `${data.clients.filter(client => clientDebt(client.id) > 0).length} clientes con saldo`; document.getElementById('metric-clients').textContent = data.clients.length;
  const chart = document.getElementById('sales-chart'), days = [...Array(7)].map((_, index) => { const date = new Date(); date.setDate(date.getDate() - 6 + index); return date.toISOString().slice(0,10); }); const values = days.map(day => data.sales.filter(sale => sale.date === day).reduce((sum, sale) => sum + sale.total, 0)); const max = Math.max(...values, 1);
  chart.innerHTML = days.map((day, index) => `<div class="bar-col"><div class="bar ${index === 6 ? 'today' : ''}" style="height:${Math.max(values[index] / max * 82, 4)}%" title="${money(values[index])}"></div><label>${new Intl.DateTimeFormat('es-MX',{weekday:'short'}).format(new Date(day+'T12:00:00')).replace('.','')}</label></div>`).join('');
  const debts = data.clients.filter(client => clientDebt(client.id) > 0).sort((a,b) => clientDebt(b.id)-clientDebt(a.id)).slice(0,4); document.getElementById('debts-preview').innerHTML = debts.length ? debts.map(client => `<div class="debt-row"><div class="person"><span class="avatar">${esc(client.name.charAt(0).toUpperCase())}</span><div><strong>${esc(client.name)}</strong><small>${esc(client.phone || 'Sin teléfono')}</small></div></div><span class="amount">${money(clientDebt(client.id))}</span></div>`).join('') : '<p class="empty">No hay saldos pendientes.</p>';
  const recent = [...data.sales].sort((a,b) => b.createdAt - a.createdAt).slice(0,5); document.getElementById('recent-sales').innerHTML = saleTable(recent, false);
}
function saleTable(sales, full) { if (!sales.length) return '<p class="empty">Todavía no hay ventas registradas.</p>'; return `<table class="data-table"><thead><tr><th>Cliente</th><th>Fecha</th><th>Total</th><th>Estado</th>${full ? '<th></th>' : ''}</tr></thead><tbody>${sales.map(sale => { const status = saleStatus(sale), client = clientById(sale.clientId); return `<tr><td><strong>${esc(client?.name || 'Venta de mostrador')}</strong></td><td>${dateLabel(sale.date)}</td><td>${money(sale.total)}</td><td><span class="status ${status[1]}">${status[0]}</span></td>${full ? `<td><button class="text-button" data-print="${sale.id}">Imprimir</button></td>` : ''}</tr>`; }).join('')}</tbody></table>`; }
function renderSales() { document.getElementById('sales-table').innerHTML = saleTable([...data.sales].sort((a,b) => b.createdAt-a.createdAt), true); }
function renderClients() { const clients = document.getElementById('clients-table'); clients.innerHTML = data.clients.length ? `<table class="data-table"><thead><tr><th>Cliente</th><th>Contacto</th><th>Compras</th><th>Saldo pendiente</th><th></th></tr></thead><tbody>${data.clients.map(client => `<tr><td><strong>${esc(client.name)}</strong></td><td>${esc(client.phone || 'Sin teléfono')}</td><td>${money(data.sales.filter(sale => sale.clientId === client.id).reduce((sum,sale)=>sum+sale.total,0))}</td><td><span class="amount">${money(clientDebt(client.id))}</span></td><td>${clientDebt(client.id) > 0 ? `<button class="text-button" data-client-payment="${client.id}">Registrar abono</button>` : ''}</td></tr>`).join('')}</tbody></table>` : '<p class="empty">Agrega clientes para controlar sus saldos.</p>'; }
function renderPayments() { const payments = [...data.payments].sort((a,b)=>b.createdAt-a.createdAt); document.getElementById('payments-table').innerHTML = payments.length ? `<table class="data-table"><thead><tr><th>Cliente</th><th>Fecha</th><th>Monto</th><th>Nota</th></tr></thead><tbody>${payments.map(payment => `<tr><td><strong>${esc(clientById(payment.clientId)?.name || 'Cliente eliminado')}</strong></td><td>${dateLabel(payment.date)}</td><td>${money(payment.amount)}</td><td>${esc(payment.note || 'Sin nota')}</td></tr>`).join('')}</tbody></table>` : '<p class="empty">Todavía no hay abonos registrados.</p>'; }
function renderAll() { renderDashboard(); renderSales(); renderClients(); renderPayments(); }

function saleForm() { openModal(`<div class="modal-header"><div><p class="eyebrow">NUEVO REGISTRO</p><h2>Registrar venta</h2></div><button class="close" data-close>×</button></div><form id="sale-form"><div class="form-grid"><div class="field full"><label>Cliente</label><select name="clientId"><option value="">Venta de mostrador</option>${data.clients.map(client=>`<option value="${client.id}">${esc(client.name)}</option>`).join('')}</select></div><div class="field full"><label>Descripción</label><input required name="description" placeholder="Ej. 100 impresiones a color" /></div><div class="field"><label>Total cobrado</label><input required type="number" min="0" step="0.01" name="total" placeholder="0.00" /></div><div class="field"><label>Costo</label><input required type="number" min="0" step="0.01" name="cost" placeholder="0.00" /></div><div class="field"><label>Pagó hoy</label><input required type="number" min="0" step="0.01" name="paid" placeholder="0.00" /></div><div class="field"><label>Fecha</label><input required type="date" name="date" value="${new Date().toISOString().slice(0,10)}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar venta</button></div></form></div>`); document.getElementById('sale-form').onsubmit = event => { event.preventDefault(); const form = new FormData(event.target), total = Number(form.get('total')), paid = Math.min(Number(form.get('paid')), total); data.sales.push({ id: crypto.randomUUID(), clientId: form.get('clientId'), description: form.get('description'), total, cost: Number(form.get('cost')), paid, date: form.get('date'), createdAt: Date.now() }); save(); closeModal(); renderAll(); showToast('Venta guardada correctamente'); }; }
function saleForm() { openModal(`<div class="modal-header"><div><p class="eyebrow">NUEVO REGISTRO</p><h2>Registrar venta</h2></div><button class="close" data-close>×</button></div><form id="sale-form"><div class="form-grid"><div class="field full"><label>Cliente</label><select name="clientId"><option value="">Venta de mostrador</option>${data.clients.map(client=>`<option value="${client.id}">${esc(client.name)}</option>`).join('')}</select></div><div class="field full"><label>Servicios de esta venta</label><div id="sale-items"></div><button type="button" class="secondary-button" id="add-sale-item">＋ Agregar otro servicio</button></div><div class="field"><label>Total de venta</label><input required readonly type="number" min="0" step="0.01" name="total" placeholder="Agrega servicios" /></div><div class="field"><label>Costo</label><input required type="number" min="0" step="0.01" name="cost" placeholder="0.00" /></div><div class="field"><label>Pagó hoy</label><input required type="number" min="0" step="0.01" name="paid" placeholder="0.00" /></div><div class="field"><label>Fecha</label><input required type="date" name="date" value="${new Date().toISOString().slice(0,10)}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar venta</button></div></form></div>`); const formElement = document.getElementById('sale-form'), itemsElement = document.getElementById('sale-items'); let itemCount = 0; const optionMarkup = () => `<option value="">Selecciona el servicio</option>${saleOptions.map((option, index)=>`<option value="${index}">${esc(option.name)}</option>`).join('')}`; const addItem = () => { itemCount += 1; itemsElement.insertAdjacentHTML('beforeend', `<div class="sale-item" data-item="${itemCount}"><select required name="service-${itemCount}">${optionMarkup()}</select><input required type="number" min="1" step="1" name="pages-${itemCount}" placeholder="Cantidad" /><span class="item-price">RD$0.00</span>${itemCount > 1 ? `<button type="button" class="remove-item" data-remove-item="${itemCount}">×</button>` : ''}</div>`); }; const updateTotal = () => { let total = 0; itemsElement.querySelectorAll('.sale-item').forEach(item => { const id = item.dataset.item, price = priceForPages(Number(formElement.elements[`service-${id}`].value), Number(formElement.elements[`pages-${id}`].value)); item.querySelector('.item-price').textContent = money(price); total += price; }); formElement.elements.total.value = total || ''; }; addItem(); document.getElementById('add-sale-item').onclick = addItem; itemsElement.addEventListener('input', updateTotal); itemsElement.addEventListener('change', updateTotal); itemsElement.addEventListener('click', event => { const removeButton = event.target.closest('[data-remove-item]'); if (removeButton) { document.querySelector(`[data-item="${removeButton.dataset.removeItem}"]`).remove(); updateTotal(); } }); formElement.onsubmit = event => { event.preventDefault(); const form = new FormData(event.target), items = [...itemsElement.querySelectorAll('.sale-item')].map(item => { const id = item.dataset.item; return { option: saleOptions[Number(form.get(`service-${id}`))], pages: Number(form.get(`pages-${id}`)) }; }), total = Number(form.get('total')), paid = Math.min(Number(form.get('paid')), total); if (!items.length || items.some(item => !item.option || !item.pages)) return showToast('Completa todos los servicios'); data.sales.push({ id: crypto.randomUUID(), clientId: form.get('clientId'), description: items.map(item => `${item.pages} × ${item.option.name}`).join(', '), items, total, cost: Number(form.get('cost')), paid, date: form.get('date'), createdAt: Date.now() }); save(); closeModal(); renderAll(); showToast('Venta guardada correctamente'); }; }
function saleForm() { openModal(`<div class="modal-header"><div><p class="eyebrow">NUEVO REGISTRO</p><h2>Registrar venta</h2></div><button class="close" data-close>×</button></div><form id="sale-form"><div class="form-grid"><div class="field full"><label>Cliente</label><select name="clientId"><option value="">Venta de mostrador</option>${data.clients.map(client=>`<option value="${client.id}">${esc(client.name)}</option>`).join('')}</select></div><div class="field full"><label>Servicio</label><select required name="service"><option value="">Selecciona el tipo de impresión</option>${saleOptions.map((option, index)=>`<option value="${index}">${esc(option.name)}</option>`).join('')}</select></div><div class="field"><label>Cantidad de páginas</label><input required type="number" min="1" step="1" name="pages" placeholder="Ej. 25" /></div><div class="field"><label>Precio por página</label><input readonly name="unitPrice" placeholder="Se calcula automáticamente" /></div><div class="field"><label>Total de venta</label><input required readonly type="number" min="0" step="0.01" name="total" placeholder="Selecciona servicio y páginas" /></div><div class="field"><label>Costo</label><input required type="number" min="0" step="0.01" name="cost" placeholder="0.00" /></div><div class="field"><label>Pagó hoy</label><input required type="number" min="0" step="0.01" name="paid" placeholder="0.00" /></div><div class="field"><label>Fecha</label><input required type="date" name="date" value="${new Date().toISOString().slice(0,10)}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar venta</button></div></form></div>`); const formElement = document.getElementById('sale-form'); const updatePrice = () => { const optionIndex = Number(formElement.elements.service.value); const pages = Number(formElement.elements.pages.value); const price = priceForPages(optionIndex, pages); formElement.elements.unitPrice.value = price && pages ? money(price / pages) : ''; formElement.elements.total.value = price || ''; }; formElement.elements.service.onchange = updatePrice; formElement.elements.pages.oninput = updatePrice; formElement.onsubmit = event => { event.preventDefault(); const form = new FormData(event.target), total = Number(form.get('total')), paid = Math.min(Number(form.get('paid')), total), option = saleOptions[Number(form.get('service'))]; data.sales.push({ id: crypto.randomUUID(), clientId: form.get('clientId'), description: `${option.name} – ${form.get('pages')} páginas`, service: option.name, pages: Number(form.get('pages')), total, cost: Number(form.get('cost')), paid, date: form.get('date'), createdAt: Date.now() }); save(); closeModal(); renderAll(); showToast('Venta guardada correctamente'); }; }
function clientForm() { openModal(`<div class="modal-header"><div><p class="eyebrow">CARTERA</p><h2>Nuevo cliente</h2></div><button class="close" data-close>×</button></div><form id="client-form"><div class="form-grid"><div class="field full"><label>Nombre completo</label><input required name="name" placeholder="Ej. María González" /></div><div class="field"><label>Teléfono</label><input name="phone" placeholder="55 0000 0000" /></div><div class="field"><label>Correo</label><input type="email" name="email" placeholder="correo@ejemplo.com" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar cliente</button></div></form></div>`); document.getElementById('client-form').onsubmit = event => { event.preventDefault(); const form = new FormData(event.target); data.clients.push({ id: crypto.randomUUID(), name: form.get('name'), phone: form.get('phone'), email: form.get('email') }); save(); closeModal(); renderAll(); showToast('Cliente agregado'); }; }
function paymentForm(clientId = '') { const options = data.clients.filter(client=>clientDebt(client.id)>0).map(client=>`<option value="${client.id}" ${client.id===clientId?'selected':''}>${esc(client.name)} · ${money(clientDebt(client.id))}</option>`).join(''); openModal(`<div class="modal-header"><div><p class="eyebrow">COBRANZA</p><h2>Registrar abono</h2></div><button class="close" data-close>×</button></div><form id="payment-form"><div class="form-grid"><div class="field full"><label>Cliente</label><select required name="clientId"><option value="">Selecciona un cliente</option>${options}</select></div><div class="field"><label>Monto recibido</label><input required type="number" min="0.01" step="0.01" name="amount" placeholder="0.00" /></div><div class="field"><label>Fecha</label><input required type="date" name="date" value="${new Date().toISOString().slice(0,10)}" /></div><div class="field full"><label>Nota</label><input name="note" placeholder="Ej. Transferencia" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar abono</button></div></form></div>`); document.getElementById('payment-form').onsubmit = event => { event.preventDefault(); const form = new FormData(event.target), client = clientById(form.get('clientId')), amount = Number(form.get('amount')); if (!client || amount > clientDebt(client.id) + .01) return showToast('El abono supera el saldo pendiente'); data.payments.push({ id: crypto.randomUUID(), clientId: client.id, amount, date: form.get('date'), note: form.get('note'), createdAt: Date.now() }); let remaining = amount; data.sales.filter(sale=>sale.clientId===client.id && sale.paid<sale.total).sort((a,b)=>a.createdAt-b.createdAt).forEach(sale=>{ const applied = Math.min(remaining, sale.total-sale.paid); sale.paid += applied; remaining -= applied; }); save(); closeModal(); renderAll(); showToast('Abono registrado'); }; }

document.addEventListener('click', event => { const nav = event.target.closest('[data-view]'); if (nav) navigate(nav.dataset.view); const target = event.target.closest('[data-view-target]'); if (target) navigate(target.dataset.viewTarget); if (event.target.closest('[data-close]') || event.target.id === 'modal-backdrop') closeModal(); if (event.target.closest('#new-sale') || event.target.closest('#new-sale-top')) saleForm(); if (event.target.closest('#new-client')) clientForm(); if (event.target.closest('#new-payment')) paymentForm(); const payment = event.target.closest('[data-client-payment]'); if (payment) paymentForm(payment.dataset.clientPayment); const print = event.target.closest('[data-print]'); if (print) printReceipt(print.dataset.print); });
function printReceipt(id) { const sale = data.sales.find(item=>item.id===id), client = clientById(sale.clientId); const printWindow = window.open('', '_blank', 'width=420,height=600'); printWindow.document.write(`<html><head><title>Comprobante PrintB</title><style>body{font:14px Arial;padding:24px;color:#18352e}h1{font-size:23px;margin:0 0 5px}p{margin:7px 0}hr{border:0;border-top:1px dashed #aaa;margin:20px 0}.total{font-size:20px;font-weight:bold}</style></head><body><h1>PrintB</h1><p>Comprobante de venta</p><hr><p><b>Fecha:</b> ${dateLabel(sale.date)}</p><p><b>Cliente:</b> ${esc(client?.name || 'Mostrador')}</p><p>${esc(sale.description)}</p><hr><p>Total: <span class="total">${money(sale.total)}</span></p><p>Pagado: ${money(sale.paid)}</p><p>Pendiente: ${money(sale.total-sale.paid)}</p><script>window.print();</script></body></html>`); printWindow.document.close(); }
renderAll();
function renderSales() { const salesTable = document.getElementById('sales-table'); const sales = [...data.sales].sort((a,b) => b.createdAt-a.createdAt); salesTable.innerHTML = sales.length ? `<table class="data-table"><thead><tr><th>Cliente</th><th>Detalle</th><th>Fecha</th><th>Total</th><th>Estado</th><th></th></tr></thead><tbody>${sales.map(sale => { const status = saleStatus(sale), client = clientById(sale.clientId); return `<tr><td><strong>${esc(client?.name || 'Venta de mostrador')}</strong></td><td>${esc(sale.description)}</td><td>${dateLabel(sale.date)}</td><td>${money(sale.total)}</td><td><span class="status ${status[1]}">${status[0]}</span></td><td><button class="text-button" data-edit-sale="${sale.id}">Editar</button> <button class="text-button" data-print="${sale.id}">Imprimir</button></td></tr>`; }).join('')}</tbody></table>` : '<p class="empty">Todavía no hay ventas registradas.</p>'; }
function editSaleForm(saleId) { const sale = data.sales.find(item => item.id === saleId); if (!sale) return; openModal(`<div class="modal-header"><div><p class="eyebrow">HISTORIAL</p><h2>Editar venta</h2></div><button class="close" data-close>×</button></div><form id="edit-sale-form"><div class="form-grid"><div class="field full"><label>Cliente</label><input readonly value="${esc(clientById(sale.clientId)?.name || 'Venta de mostrador')}" /></div><div class="field full"><label>Descripción</label><input required name="description" value="${esc(sale.description)}" /></div><div class="field"><label>Total de venta</label><input required type="number" min="0" step="0.01" name="total" value="${sale.total}" /></div><div class="field"><label>Pagó</label><input required type="number" min="0" step="0.01" name="paid" value="${sale.paid}" /></div><div class="field full"><label>Fecha</label><input required type="date" name="date" value="${sale.date}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar cambios</button></div></form></div>`); document.getElementById('edit-sale-form').onsubmit = event => { event.preventDefault(); const form = new FormData(event.target); sale.description = form.get('description'); sale.total = Number(form.get('total')); sale.paid = Math.min(Number(form.get('paid')), sale.total); sale.date = form.get('date'); save(); closeModal(); renderAll(); showToast('Venta actualizada'); }; }
document.addEventListener('click', event => { const edit = event.target.closest('[data-edit-sale]'); if (edit) editSaleForm(edit.dataset.editSale); });
document.addEventListener('click', event => { if (event.target.closest('#new-sale') || event.target.closest('#new-sale-top')) setTimeout(() => { const cost = document.querySelector('#sale-form input[name="cost"]'); if (cost) { cost.value = '0'; cost.required = false; cost.closest('.field').style.display = 'none'; } }, 0); });
document.getElementById('export-data').onclick = () => { const file = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' }); const link = document.createElement('a'); link.href = URL.createObjectURL(file); link.download = `printb-respaldo-${new Date().toISOString().slice(0, 10)}.json`; link.click(); URL.revokeObjectURL(link.href); showToast('Respaldo descargado'); };
document.getElementById('import-data').onclick = () => document.getElementById('import-file').click();
document.getElementById('import-file').onchange = async event => { const file = event.target.files[0]; if (!file) return; try { const imported = JSON.parse(await file.text()); if (!Array.isArray(imported.sales) || !Array.isArray(imported.clients) || !Array.isArray(imported.payments)) throw new Error('Formato inválido'); data = imported; save(); renderAll(); showToast('Datos restaurados correctamente'); } catch { showToast('El archivo no es un respaldo válido'); } event.target.value = ''; };

function renderClients() { const clients = document.getElementById('clients-table'); clients.innerHTML = data.clients.length ? `<table class="data-table"><thead><tr><th>Cliente</th><th>Contacto</th><th>Compras</th><th>Saldo pendiente</th><th></th></tr></thead><tbody>${data.clients.map(client => `<tr><td><strong>${esc(client.name)}</strong></td><td>${esc(client.phone || 'Sin teléfono')}</td><td>${money(data.sales.filter(sale => sale.clientId === client.id).reduce((sum,sale)=>sum+sale.total,0))}</td><td><span class="amount">${money(clientDebt(client.id))}</span></td><td><button class="text-button" data-edit-client="${client.id}">Editar</button>${clientDebt(client.id) > 0 ? ` <button class="text-button" data-client-payment="${client.id}">Registrar abono</button>` : ''}</td></tr>`).join('')}</tbody></table>` : '<p class="empty">Agrega clientes para controlar sus saldos.</p>'; }
function editClientForm(clientId) { const client = clientById(clientId); if (!client) return; openModal(`<div class="modal-header"><div><p class="eyebrow">CARTERA</p><h2>Editar cliente</h2></div><button class="close" data-close>×</button></div><form id="edit-client-form"><div class="form-grid"><div class="field full"><label>Nombre completo</label><input required name="name" value="${esc(client.name)}" /></div><div class="field"><label>Teléfono</label><input name="phone" value="${esc(client.phone || '')}" /></div><div class="field"><label>Correo</label><input type="email" name="email" value="${esc(client.email || '')}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar cambios</button></div></form></div>`); document.getElementById('edit-client-form').onsubmit = event => { event.preventDefault(); const form = new FormData(event.target); client.name = form.get('name'); client.phone = form.get('phone'); client.email = form.get('email'); save(); closeModal(); renderAll(); showToast('Cliente actualizado'); }; }
document.addEventListener('click', event => { const edit = event.target.closest('[data-edit-client]'); if (edit) editClientForm(edit.dataset.editClient); });
function renderSales() { const salesTable = document.getElementById('sales-table'); const sales = [...data.sales].sort((a,b) => b.createdAt-a.createdAt); salesTable.innerHTML = sales.length ? `<table class="data-table"><thead><tr><th>Cliente</th><th>Detalle</th><th>Fecha</th><th>Total</th><th>Estado</th><th></th></tr></thead><tbody>${sales.map(sale => { const status = saleStatus(sale), client = clientById(sale.clientId), details = sale.items?.length ? sale.items.map(item => `<li>${esc(item.pages)} × ${esc(item.option?.name || '')}</li>`).join('') : esc(sale.description).split(', ').map(item => `<li>${item}</li>`).join(''); return `<tr><td><strong>${esc(client?.name || 'Venta de mostrador')}</strong></td><td><ul style="margin:0;padding-left:18px">${details}</ul></td><td>${dateLabel(sale.date)}</td><td>${money(sale.total)}</td><td><span class="status ${status[1]}">${status[0]}</span></td><td><button class="text-button" data-edit-sale="${sale.id}">Editar</button> <button class="text-button" data-print="${sale.id}">Imprimir</button></td></tr>`; }).join('')}</tbody></table>` : '<p class="empty">Todavía no hay ventas registradas.</p>'; }
function editSaleForm(saleId) { const sale = data.sales.find(item => item.id === saleId); if (!sale) return; openModal(`<div class="modal-header"><div><p class="eyebrow">HISTORIAL</p><h2>Editar venta</h2></div><button class="close" data-close>×</button></div><form id="edit-sale-form"><div class="form-grid"><div class="field full"><label>Cliente</label><select required name="clientId"><option value="">Venta de mostrador</option>${data.clients.map(client=>`<option value="${client.id}" ${client.id === sale.clientId ? 'selected' : ''}>${esc(client.name)}</option>`).join('')}</select></div><div class="field full"><label>Descripción</label><input required name="description" value="${esc(sale.description)}" /></div><div class="field"><label>Total de venta</label><input required type="number" min="0" step="0.01" name="total" value="${sale.total}" /></div><div class="field"><label>Pagó</label><input required type="number" min="0" step="0.01" name="paid" value="${sale.paid}" /></div><div class="field full"><label>Fecha</label><input required type="date" name="date" value="${sale.date}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar cambios</button></div></form></div>`); document.getElementById('edit-sale-form').onsubmit = event => { event.preventDefault(); const form = new FormData(event.target); sale.clientId = form.get('clientId'); sale.description = form.get('description'); sale.total = Number(form.get('total')); sale.paid = Math.min(Number(form.get('paid')), sale.total); sale.date = form.get('date'); save(); closeModal(); renderAll(); showToast('Venta actualizada'); }; }

function saleForm() { openModal(`<div class="modal-header"><div><p class="eyebrow">NUEVO REGISTRO</p><h2>Registrar venta</h2></div><button class="close" data-close>×</button></div><form id="sale-form"><div class="form-grid"><div class="field full"><label>Cliente</label><select name="clientId"><option value="">Venta de mostrador</option>${data.clients.map(client=>`<option value="${client.id}">${esc(client.name)}</option>`).join('')}</select></div><div class="field full"><label>Servicios de esta venta</label><div id="sale-items"></div><button type="button" class="secondary-button" id="add-sale-item">＋ Agregar otro servicio</button></div><div class="field"><label>Total de venta</label><input required readonly type="number" min="0" step="0.01" name="total" placeholder="Agrega servicios" /></div><div class="field"><label>Costo</label><input required type="number" min="0" step="0.01" name="cost" placeholder="0.00" /></div><div class="field"><label>Pagó hoy</label><input required type="number" min="0" step="0.01" name="paid" placeholder="0.00" /></div><div class="field"><label>Fecha</label><input required type="date" name="date" value="${new Date().toISOString().slice(0,10)}" /></div></div><div class="modal-actions"><button type="button" class="secondary-button" data-close>Cancelar</button><button class="primary-button">Guardar venta</button></div></form></div>`); const formElement = document.getElementById('sale-form'), itemsElement = document.getElementById('sale-items'); let itemCount = 0; const optionMarkup = () => `<option value="">Selecciona el servicio</option>${saleOptions.map((option, index)=>`<option value="${index}">${esc(option.name)}</option>`).join('')}`; const addItem = () => { itemCount += 1; itemsElement.insertAdjacentHTML('beforeend', `<div class="sale-item" data-item="${itemCount}"><select required name="service-${itemCount}">${optionMarkup()}</select><input required type="number" min="1" step="1" name="pages-${itemCount}" placeholder="Cantidad" /><span class="item-price">RD$0.00</span>${itemCount > 1 ? `<button type="button" class="remove-item" data-remove-item="${itemCount}">×</button>` : ''}</div>`); }; const updateTotal = () => { let total = 0; itemsElement.querySelectorAll('.sale-item').forEach(item => { const id = item.dataset.item, price = priceForPages(Number(formElement.elements[`service-${id}`].value), Number(formElement.elements[`pages-${id}`].value)); item.querySelector('.item-price').textContent = money(price); total += price; }); formElement.elements.total.value = total || ''; }; addItem(); document.getElementById('add-sale-item').onclick = addItem; itemsElement.addEventListener('input', updateTotal); itemsElement.addEventListener('change', updateTotal); itemsElement.addEventListener('click', event => { const removeButton = event.target.closest('[data-remove-item]'); if (removeButton) { document.querySelector(`[data-item="${removeButton.dataset.removeItem}"]`).remove(); updateTotal(); } }); formElement.onsubmit = event => { event.preventDefault(); const form = new FormData(event.target), items = [...itemsElement.querySelectorAll('.sale-item')].map(item => { const id = item.dataset.item; return { option: saleOptions[Number(form.get(`service-${id}`))], pages: Number(form.get(`pages-${id}`)) }; }), total = Number(form.get('total')), paid = Math.min(Number(form.get('paid')), total); if (!items.length || items.some(item => !item.option || !item.pages)) return showToast('Completa todos los servicios'); data.sales.push({ id: crypto.randomUUID(), clientId: form.get('clientId'), description: items.map(item => `${item.pages} × ${item.option.name}`).join(', '), items, total, cost: Number(form.get('cost')), paid, date: form.get('date'), createdAt: Date.now() }); save(); closeModal(); renderAll(); showToast('Venta guardada correctamente'); }; }
